import { GoogleAuth } from "google-auth-library";
import { type Message } from "./types";
export declare class FCM {
    private auth;
    protected client: Awaited<ReturnType<GoogleAuth["getClient"]>> | null;
    private url;
    constructor(path_to_creds: string);
    getAccessToken(): Promise<string | null | undefined>;
    send(registrationToken: string, message: Message): Promise<{
        name: string;
    }>;
    sendAll(registrationTokens: string[], message: Message): Promise<{
        name: string;
    }[] | undefined>;
    sendToTopic(topic: string, message: Message): Promise<any>;
}
